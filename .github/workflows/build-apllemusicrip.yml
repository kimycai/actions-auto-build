name: Build apple-music-rip (Ubuntu 24.04)
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch: # 手动触发，优先级最高

# 并发控制：避免同分支重复构建
concurrency:
  group: ${{ github.ref }}-apple-music-rip-ubuntu2404
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-24.04 # 固定Ubuntu 24.04官方运行器
    steps:
      - name: 1. 强制检出原项目完整代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          repository: HARAJIT05/apple-music-rip
          ref: main
          path: .

      - name: 2. 修复Ubuntu 24.04依赖锁&更新软件源
        run: |
          sudo rm -f /var/lib/dpkg/lock-frontend
          sudo dpkg --configure -a
          sudo apt update -y && sudo apt upgrade -y --no-install-recommends
          sudo apt clean

      - name: 3. 安装项目必要系统依赖（无Java、无冗余）
        run: |
          sudo apt install -y --no-install-recommends \
            build-essential \
            git \
            curl \
            wget \
            unzip \
            zip \
            python3 \
            python3-pip \
            python3-venv \
            libssl-dev \
            pkg-config \
            golang-go \
            net-tools
          sudo apt autoremove -y && sudo apt clean
          echo "Python版本：$(python3 --version)"
          echo "Go版本：$(go version)"

      - name: 4. 配置Python虚拟环境&安装Web核心依赖
        run: |
          cd $GITHUB_WORKSPACE
          python3 -m venv .venv
          source .venv/bin/activate
          pip install --upgrade pip
          pip install flask==2.3.3 flask-cors==4.0.0 requests==2.31.0
          pip freeze > requirements.txt

      - name: 5. 核心修复：替换为无Nix、缩进合规的main.py（彻底解决IndentationError）
        run: |
          cd $GITHUB_WORKSPACE
          # 备份原main.py（可选，便于排查）
          mv main.py main.py.bak
          # 创建全新的、无Nix逻辑、缩进合规的main.py，覆盖原文件
          cat > main.py << 'EOF'
import os
import subprocess
import sys
from app import create_app

app = create_app()

# 检查必要的系统依赖
def check_dependencies():
    required_tools = ["git", "go", "python3", "pip3"]
    missing = []
    for tool in required_tools:
        if subprocess.call(["which", tool], stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL) != 0:
            missing.append(tool)
    if missing:
        print(f"❌ 缺少必要依赖：{', '.join(missing)}，请先安装")
        sys.exit(1)
    print("✅ 所有系统依赖检查通过")

# 拉取apple-music-downloader核心工具
def pull_downloader():
    if not os.path.exists("apple-music-downloader"):
        print("🔍 正在拉取apple-music-downloader核心工具...")
        subprocess.run(["git", "clone", "https://github.com/zhaarey/apple-music-downloader.git"], check=True)
        print("✅ apple-music-downloader拉取完成")
    else:
        print("✅ apple-music-downloader已存在，无需重新拉取")

# 拉取wrapper鉴权工具
def pull_wrapper():
    if not os.path.exists("wrapper"):
        print("🔍 正在拉取wrapper鉴权工具...")
        subprocess.run(["git", "clone", "https://github.com/WorldObservationLog/wrapper.git"], check=True)
        print("✅ wrapper拉取完成")
    else:
        print("✅ wrapper已存在，无需重新拉取")

# 初始化项目（拉取所有核心工具）
def init_project():
    check_dependencies()
    pull_downloader()
    pull_wrapper()
    print("🎉 项目初始化完成，所有核心工具已就绪")

if __name__ == "__main__":
    # 初始化项目
    init_project()
    # 启动Flask Web服务（默认端口5000，所有IP可访问）
    app.run(host="0.0.0.0", port=5000, debug=False)
EOF
          # 添加执行权限
          chmod +x main.py
          # 验证新main.py语法（提前拦截问题）
          python3 -m py_compile main.py
          if [ $? -eq 0 ]; then
            echo "✅ 新main.py创建成功，语法检查通过（无缩进/nix错误）"
          else
            echo "❌ 新main.py语法错误"
            exit 1
          fi
        shell: /usr/bin/bash -e {0}

      - name: 6. 项目初始化（拉取核心工具，不启动服务）
        run: |
          cd $GITHUB_WORKSPACE
          source .venv/bin/activate
          # 仅执行初始化，拉取工具后退出
          python3 -c "from main import init_project; init_project()"
          echo "✅ 核心工具（apple-music-downloader/wrapper）拉取完成"

      - name: 7. 打包完整可运行产物（统一路径+环境变量导出）
        run: |
          cd $GITHUB_WORKSPACE
          mkdir -p dist
          # 复制所有核心文件（容错处理缺失目录）
          cp -r app/ main.py requirements.txt dist/ 2>/dev/null
          cp -r .venv/ dist/ 2>/dev/null
          cp -r apple-music-downloader/ dist/ 2>/dev/null
          cp -r wrapper/ dist/ 2>/dev/null
          # 定义固定产物名称，避免路径问题
          PACKAGE_NAME="apple-music-rip-ubuntu2404-build.zip"
          zip -r "${PACKAGE_NAME}" dist/
          # 校验产物是否生成
          if [ ! -f "${PACKAGE_NAME}" ]; then
            echo "❌ 产物打包失败"
            ls -lh
            exit 1
          fi
          # 导出产物绝对路径
          echo "PACKAGE_PATH=$GITHUB_WORKSPACE/${PACKAGE_NAME}" >> $GITHUB_ENV
          ls -lh "${PACKAGE_NAME}"
          echo "✅ 产物打包完成：${PACKAGE_NAME}"

      - name: 8. 上传构建产物（精准匹配路径）
        uses: actions/upload-artifact@v4
        with:
          name: apple-music-rip-ubuntu2404-build
          path: ${{ env.PACKAGE_PATH }}
          retention-days: 90
          if-no-files-found: error

      - name: 9. 验证产物运行（语法检查+端口验证）
        run: |
          cd $GITHUB_WORKSPACE/dist
          source .venv/bin/activate
          # 第一步：强制Python语法检查
          echo "🔍 执行Python语法检查"
          python3 -m py_compile main.py
          if [ $? -ne 0 ]; then
            echo "❌ main.py语法检查失败"
            exit 1
          fi
          echo "✅ main.py语法检查通过"
          # 第二步：后台启动项目
          python3 main.py 2>&1 > run.log &
          sleep 8 # 延长启动时间，确保Flask加载完成
          # 第三步：精准检查5000端口（仅匹配Python3进程）
          if netstat -tulpn 2>/dev/null | grep -E "(:5000).*python3"; then
            echo "✅ 产物验证成功：5000端口已被Python3监听"
          else
            echo "❌ 产物验证失败，运行日志："
            cat run.log || echo "无运行日志"
            exit 1
          fi
          # 安全停止服务
          pkill -f "python3 main.py" 2>/dev/null
        shell: /usr/bin/bash -e {0}
